package pszerszenowicz.application.invitation;

public class InvitationService {
    /*
    private final InvitationRepository repo;
    private final GameService gameService;

    public InvitationId invite(UserId from, UserId to, GameType game) {
        GameInvitation invitation = new GameInvitation(from, to, game);
        repo.save(invitation);
        return invitation.id();
    }

    public void accept(InvitationId id, UserId user) {
        GameInvitation inv = repo.get(id);
        inv.accept(user);
        repo.save(inv);

        gameService.startGame(inv);
    }
     */
}
